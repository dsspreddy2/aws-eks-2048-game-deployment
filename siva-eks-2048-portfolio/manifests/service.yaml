apiVersion: v1
kind: Service
metadata:
  name: service-2048
  namespace: game-2048
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: game-2048
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
